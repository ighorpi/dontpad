version: '3.9'
services:
  app:
    image: docker pull ghcr.io/ighorpi/gojo-pharaoh-bot:latest
    container_name: dontpad
    build:
      context: .
      dockerfile: Dockerfile
    ports:
    - "3000:3000"
    environment:
      NODE_ENV: production
      DATABASE_URL: '${DATABASE_URL}'
